I want you to read OpenDelto and fast-der-solver.  Then I want you to think about creating a solver for OpenDleto that uses the method of fast-der-solve.  Study quick-der-solve to see how it is done.  Remember we need to sample only some random linear combination of the solutions to derivations to use stratify in OpenDleto.  Then I want the option to select that solver from others and I want to have a Jupyter notebook that loads some spheres from SphereLab, randomizes, then stratifies under the two solutions.  Think about how to refactor the OpenDleto framework to have multiple options.  In the end I want a new branch of the OpenDleto package that fully includes the fast-der-solver strategies.  At first they may be written for 3-valent tensors only.  That is ok to limit the new solver to this use case.  Think deeply, do a good job.  Create an agent to check on your work and make sure it passes all tests.